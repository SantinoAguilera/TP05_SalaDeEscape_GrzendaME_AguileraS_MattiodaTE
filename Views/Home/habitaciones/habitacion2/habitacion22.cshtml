@{
    ViewData["Title"] = "habitacion22";
}
<body class="bodyGame">
    <main class="game">
        @{
            string url = "/images/rooms/sala22" + ViewBag.estadoSalaID + ".jpg";
        }
        <img src="@url" class="mainimg">

        <!-- CAMBIO DE POSICION -->
        <a href='@Url.Action("Habitacion", "Home", new{sala=21})' class="arrow arrow2221" tabindex="-1"></a>

        <!-- HITBOX DEL MARCADOR AZUL -->
        <a href='@Url.Action("Marcador", "Home", new{sala=22, marcador="azul"})' class="hitbox hitboxAzul" tabindex="-1"></a>

        <!--CONTRASEÑA-->
        <form action='@Url.Action("Resolver", "Home")' autocomplete="off" tabindex="-1">
            <input type="hidden" name="sala" value="22" tabindex="-1">
            <input type="text" name="contrasena" required class="text text22" tabindex="-1">
            <input type="submit" hidden tabindex="-1">
        </form>

        @{
            if (ViewBag.estadoSalaID == 4){
                <style>
                    .text22{
                        right: 100vw;
                    }
                </style>
            }
        }
    </main>
</body>